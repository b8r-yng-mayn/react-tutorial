const Header = ({title}) => {
  return (
    <header className='header'>
      <h1>{title}</h1>
    </header>
  )
}

// CSS in JS
// const headingStyle = {
//   color: 'red',
//   backgroundColor: 'black',
// }

export default Header
